#include <stdio.h>
#include "holberton.h"

/**
 * main - test function that prints if program works properly
 */

int main(void)
{
	int i = 98;

	if (positive_or_negative(i) == 0);
	{
		printf("0 is zero\n");
	}
	return (0);
}
