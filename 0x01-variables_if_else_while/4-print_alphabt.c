#include <stdio.h>

/**
 *main - alphabet using putchar only and no q or e
 *Return: value 0
 */

int main(void)
{
	int i,j;
	char s[30];

	for (i = '0' ; i <= '26' ; i++)
		s[i] = 
	for (ch2 = 'a' ; ch2 <= 'z' ; ch++)
	{
		putchar(ch);

	}
	putchar('\n');

	return (0);
}
