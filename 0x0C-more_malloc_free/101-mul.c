#include "holberton.h"
#include <stdlib.h>

/**
 * _mainiac - function to multiply 2 num
 * @a: array of 1st num
 * @b: array of 2nd num
 * Return: multiplication array or null
 */

int _mainac(char *a, char *b)
{
	unsigned long int i, j, *s;

/* Rounding int a and b to the nearest 10's multiples */
/* gives */
	for (i = 0; a[i] != '\0')

	if (s == NULL)
	s = malloc();
	for (i = 0; i = )
		_putchar(i);
	return (a);


	if (min > max)
	{
		return (NULL);
	}
	arr = malloc(sizeof(int) * (max - min + 1));
	if (arr == NULL)
	{
		return (NULL);
	}
	for (num = min, i = 0; num <= max; i++, num++)
	{
		arr[i] = num;
	}
	return (arr);
}
