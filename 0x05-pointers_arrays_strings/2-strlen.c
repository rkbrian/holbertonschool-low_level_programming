#include "holberton.h"

/**
 * _strlen - pointer to find string length
 * @s: string input
 * Return: 0
 */

int _strlen(char *s)
{
	_putchar(sizeof(s) + '0');
	_putchar(sizeof(s) + '0');
	return (0);
}
