#include "holberton.h"

/**
 * print_rev - reverse alphabet using putchar only
 * Return: value 0
 */

void print_rev(char *s)
{
	char *i = *s;

	while (i = 0)
	{
		;

	}
	putchar('\n');

	return (0);
}
